#include<stdio.h>
int main()
{
        float R,D;
        printf("Enter amount in rupees:");
        scanf("%f",&R);
        D=R/87;
        printf("\n The amount in Dollar: %f",D);
        return 0;
}
